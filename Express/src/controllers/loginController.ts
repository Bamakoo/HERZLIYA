/**
 * A. récupérer le username + mdp dans le header Autorization
 * B. les stoker dans des variables
 * C. trouver une corespondance dnas la liste
 * D. retourner un token
 */

import { Request, Response } from "express";
// import { list } from "./usersControllers";

export const login = async (req: Request, res: Response) => {};
